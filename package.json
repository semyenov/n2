{
  "private": true,
  "workspaces": ["packages/*", "services/*", "examples/*"],
  "scripts": {
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:watch": "vitest",
    "test:services": "bun scripts/test-services.ts",
    "stress:services": "bun scripts/stress-services.ts"
  },
  "devDependencies": {
    "@effect/vitest": "^0.29.0",
    "@types/bun": "latest",
    "@vitest/coverage-v8": "3.2.4",
    "typescript": "^5",
    "vitest": "^3.2.0"
  }
}
